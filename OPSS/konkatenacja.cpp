#include<iostream>
#include<vector>
using namespace std;

int d,n,g;
unsigned long long wynik;
vector<int>tab;

void sumuj(int f)
{
for(int i=0;i<tab.size();i++)
}


main()
{
ios_base::sync_with_stdio(0);
tab.reserve(200);
cin >> d;

for(int i=0;i<d;i++)
{
   cin >> n;
   for(int a=0;a<n;a++)
   {
      cin >> g;
      tab.push_back(g);
   } 
   cout<< sumuj(0);
}

return 0;
}
